<h1>List of Users</h1>

<ul>
<% @users.each do |user| %>
	<li>
		<% if user.avatar.exists? %>
			<a href = "<%= user.avatar.url %>">Watch My Video</a>
			<!-- #image_tag user.avatar.url, :size => "50x50" -->
		<% end %>
		<%= user.name %>
		<%= user.email %>		
	</li>
<% end %>
</ul>